spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/project-ClearVolt?createDataBaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    open-in-view: false
